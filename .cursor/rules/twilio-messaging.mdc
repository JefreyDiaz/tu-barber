---
description: Twilio WhatsApp messaging integration rules
globs: "**/lib/messaging/**/*.{ts,js},**/app/api/bookings/**/*.{ts,js},**/lib/reminders.ts"
alwaysApply: false
---

# Twilio WhatsApp Messaging

## Provider

Customer-facing WhatsApp goes through **Twilio Content API** (approved utility templates).

## Required env (platform / Basic plan)

- `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_FROM`
- `TWILIO_CONTENT_SID_BOOKING`, `TWILIO_CONTENT_SID_REMINDER`

## Not WhatsApp (Resend email instead)

- New booking alert to barbería → `lib/email/notify-barber-new-booking.ts` (do **not** use `tubarber_barber_new_booking_v1`)
- Tenant approval welcome → `lib/email/notify-tenant-approved.ts` (do **not** use `tubarber_tenant_welcome_v1`)
- Subscription/trial expiry reminder to owner → `lib/email/notify-subscription-expiring-owner.ts`

## Templates in Twilio Console

| Template | SID (prod) | Usar |
|----------|------------|------|
| `tubarber_booking_confirm_v1` | `TWILIO_CONTENT_SID_BOOKING` | ✅ Confirmación al cliente |
| `tubarber_booking_reminder_v1` | `TWILIO_CONTENT_SID_REMINDER` | ✅ Recordatorio ~3h antes |
| `tubarber_barber_new_booking_v1` | — | ❌ Puede borrarse (alerta va por email) |
| `tubarber_tenant_welcome_v1` | — | ❌ Puede borrarse (bienvenida va por email) |

## Config resolution

- Tenant Cadena with `twilioAccountSid` + `twilioAuthToken` in `TenantSettings` → tenant override
- Otherwise → platform env vars

## Template variable order ({{1}}, {{2}}, … in Twilio Console)

**Booking / Reminder** (`tubarber_booking_confirm_v1`, SID `HX808125…`):
1=shopName, 2=customerName, 3=barberName, 4=bookingDate, 5=bookingTime, 6=barberPhone (10 dígitos), 7=cancelUrl

## Fail-safe

If Twilio fails, the booking must still be saved. Log with `[Twilio]` prefix.

## Phone format

Colombia: 10 digits in forms → normalized to `+57XXXXXXXXXX` via `lib/messaging/phone.ts`.
